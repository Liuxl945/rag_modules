transformers>=4.40.0
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0
sentence-transformers>=3.0.0

# LangChain 1.x 依赖（项目仅使用 Document 类 + HuggingFace 嵌入）
langchain-core>=1.4.0,<2.0.0
langchain>=1.0.0,<2.0.0
langchain-huggingface>=0.2.0,<2.0.0
unstructured-client==0.41.0
neo4j>=5.0.0

pymilvus==2.5.11
rank-bm25>=0.2.2
jieba>=0.42.1

lazy_loader==0.4
huggingface-hub>=0.33.4
tokenizers>=0.19.0
accelerate>=0.20.0

# OpenAI SDK（直接用于 DeepSeek API 调用，langchain-openai 1.x 需要 >=2.45.0）
openai>=2.45.0,<3.0.0

tiktoken>=0.4.0

python-dotenv>=1.0.0

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0
tqdm>=4.64.0
pydantic>=2.0.0
requests>=2.28.0

# Web 服务（前后端分离）
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sse-starlette>=1.6.0

# RAGAS 评估系统（可选：未安装时主应用照常运行，仅评估接口返回 503）
# 注意：langchain-community>=0.4 把 vertexai 拆到 langchain-google-vertexai，
# ragas（截至 0.4.3）仍按旧路径导入，必须锁定 <0.4
ragas>=0.2.0
langchain-community<0.4
langchain-openai>=0.4.0,<2.0.0
python-multipart