建议新增功能
🔥 高优先级 - 核心体验
食材/菜谱浏览页 - 目前只能通过聊天或知识图谱"按菜谱"查看，缺少独立的食材/菜谱浏览和搜索页。可以做一个可搜索/筛选的菜谱列表，支持按分类、菜系、难度筛选，点击直接查看详情或图谱。

检索过程可视化 - 在聊天回答中展示"为什么推荐这个结果"。当前有 AnalysisTag 显示路由策略，但缺少：

检索到了哪些 chunk/文档（已经有 SourceList，可以增强为可展开的详情面板）
图RAG的推理路径展示（走了哪些关系、多少跳）
各检索策略的分数对比
文档上传/管理 - 目前文档完全来自 Neo4j 内置数据。增加用户上传菜谱文档（PDF/文本/Markdown）功能，自动解析、切块、入向量库和图数据库。

🌟 中优先级 - 实用增强
收藏/点赞回答 - 会话中对满意的回答标记收藏，方便后续回看，也可用于后续的反馈微调。

多轮对话上下文优化 - 目前聊天可能缺少明确的上下文窗口管理，增加：

显示当前对话使用了哪些历史消息作为上下文
用户可手动编辑/删除历史消息
对话标题自动生成（已有会话但标题可能是默认的）
导出对话/分享 - 导出单条回答或整个会话为 Markdown/PDF，或生成分享链接。

对比检索模式 - 在聊天界面提供"切换检索策略"开关，让用户对比传统检索 vs 图RAG vs 组合策略的回答差异，直观展示图RAG的优势。

食材替代推荐 - 专门的"如果我没有X，可以用什么代替"功能，利用图中食材的分类/关系做替代推荐。

💡 低优先级 - 锦上添花
烹饪计时器集成 - 对含时间的烹饪步骤，一键启动倒计时。

国际化(i18n) - 目前是纯中文，可加英文支持。

暗色主题 - Element Plus 支持暗色模式，可切换。

回答反馈(👍/👎) - 用户对回答质量打分，收集数据用于后续优化路由和prompt。

批量导入菜谱 - 从JSON/CSV批量导入菜谱数据到Neo4j。

菜谱推荐 - 根据现有食材（用户输入"我有鸡蛋、番茄、土豆"），推荐可做的菜谱。